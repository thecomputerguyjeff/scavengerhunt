package com.example.scavengerhunt;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ScavengerhuntApplication {
	//This is the main that runs the Spring application, which allows the Controller to be active
	public static void main(String[] args) {
		SpringApplication.run(ScavengerhuntApplication.class, args);
	}
	//The secret word is "chasana" (since that is where I am at right now!
}
